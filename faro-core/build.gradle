plugins {
    id 'java-library'
}

dependencies {
    // Avro — schema serialisation
    api 'org.apache.avro:avro:1.11.3'

    // Jackson — JSON serialisation for feature_value bytes
    api 'com.fasterxml.jackson.core:jackson-databind:2.16.1'
}
